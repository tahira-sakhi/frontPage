name: HTML Project Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build HTML
      run: |
        # You can run any build commands here, like compiling SCSS to CSS or minifying JS.
        # For a basic HTML project, this step may not be necessary.

  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install dependencies
      run: npm install   # If you have any dependencies or testing frameworks

    - name: Run tests
      run: |
        # Run your tests here
        # For a basic HTML project, this step may not be necessary.

  release:
    name: Release
    runs-on: ubuntu-latest
    needs: [build, test]

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Release to GitHub Pages
      run: |
        # Copy your built files to a specific directory (e.g., 'docs' or 'dist')
        # This assumes that your HTML files are in the root directory of your repository.
        cp index.html docs/index.html  # Adjust this according to your project structure

    - name: Deploy to GitHub Pages
      uses: JamesIves/github-pages-deploy-action@3.7.1
      with:
        branch: gh-pages   # Specify the branch to deploy to
        folder: docs       # Specify the folder to deploy
